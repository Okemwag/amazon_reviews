# Screenshot Checklist

Capture these screenshots for the final report:

- `docker compose ps` showing Hadoop, Hive, and Spark services.
- HDFS NameNode UI at `http://localhost:9870`.
- Spark Master UI at `http://localhost:8080`.
- Hive query output from `scripts/run_hive_queries.sh`.
- `output/metrics/data_quality_metrics.json`.
- `output/reports/quality_report.txt`.
- `output/metrics/ml_metrics.json`.
- Model directory in HDFS under `/user/bigdata/amazon_reviews/models/sentiment_classifier`.
