# Raw Dataset Files

Place Amazon Reviews 2023 JSON Lines files here before running the pipeline.

For the assignment run, use a larger category file from the official Amazon
Reviews 2023 dataset, such as `Office_Products.jsonl`.

Run the full pipeline with:

```bash
make run INPUT=data/raw/Office_Products.jsonl
```
